# Empty in production means same-origin: POST /predict.
# Set this to an absolute HTTP(S) origin when the model API is hosted separately.
NEXT_PUBLIC_PIXELPROOF_API_URL=http://127.0.0.1:8799

# Public web origin used for absolute Open Graph image metadata.
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3000
