# Markdown reconciliation — 25 September 2026

## Status and method

The pre-review snapshot is `16e03d832c57cea2cea8806807660e1389fe9f1e`.
All 28 tracked Markdown files (37,073 lines, 2,956,328 bytes, 1,761 sections) were
covered by both the structural inventory and the completed sequential semantic review.
The durable ledger is `sources/markdown_read_progress.json`. Every baseline file is
complete. Exact duplicate paragraphs were cross-referenced only after the same text
had already been displayed and reviewed. Changed and newly written material was then
reviewed separately. Ignored datasets, dependencies and third-party installations are
outside this project-document scope.

The review reconciles the report with the research chronology and selected primary
receipts. It does not rerun every experiment, independently validate every source claim,
or turn historically consumed data into a fresh benchmark. Earlier claims remain in
the append-only logs, with later corrections taking precedence.

## Corrections already applied

| Finding | Primary record | Report or documentation action |
|---|---|---|
| Early resolution/camera-noise claims were too absolute | Early HISTORY, E19b/E19c, later forensic-note corrections | Retain label-error reversal and qualify mechanism claims; no universal crop immunity |
| Internal performance repeatedly failed new real pipelines | E31/E32, E33–E41 history | Explain why early working models were not the completed detector |
| E42 failed a much larger RR evaluation | `evidence/e42_rr_result.json` | Add 16,953 parents / 50,858 linked views and the failed external conditions |
| Specialist fusion improved some sources while harming others | E43–E50 history | Include reconstruction, social-real and GAN failures as the reason for later retention constraints |
| E43's E49 final was not passed by E92 | `evidence/e49_final_result.json`, E92 development receipt | Add 11/20, 391/1,000 and 490/1,000 real false alerts; explicitly separate the populations |
| E92 is the demo version, not the smallest later development FP count | `evidence/e102_development.json` | Add E102's 14-to-12/160 improvement, failed full acceptance and nonpromotion |
| Displayed cuts are inherited operating points | E48/E49 threshold-origin audit | Explain upper CAL origin and consumed lower-cut origin; no probability or universal-optimum claim |
| An unopened reserve is not automatically an independent final | E139–E146 records | Add three AI-bearing source components, unresolved ancestry and MNW/HDR+ reserve limitations |
| Old setup/report pages looked current | README, ARTIFACTS, experiment/tool indexes, old report/deck | Add explicit historical scope and links to current serving/submission sources |
| Historical network policies were easy to misread | DATASETS chronology | Label dated download authorization and later mobile-data boundary; no new data transferred |

## Artifact verification

The revised report contains 30 pages, 35 numbered headings, three figures, six tables
and fourteen references. The output audit passes 638 scoped checks. Every report page
was rendered and visually inspected. In the final editorial pass, all 16 changed pages
were inspected again; the other 14 matched the previously inspected renders exactly. All 35 contents entries match rendered locations.
The legacy DOC retains 30 pages and the same per-page word multisets; five rendering
differences were inspected. Section limits still pass (project details: nine pages).

The 15-slide presentation has 13 spoken slides and two reference slides; planned speech
is 795 seconds. Slide 9 was revised and inspected during reconciliation. The other fourteen slides and the
one-slide digest match the previously reviewed renders pixel for pixel. Both editable
decks pass package/layout/font/import checks. No native Microsoft Office test or live
rehearsal is claimed.

Seven primary artifacts and the handoff/study/notes files are copied to
`~/Desktop/CS395_PixelProof_Submission_2026`. Artifact hashes must match the repository.
The report remains a draft because ten placeholder contexts remain; the user explicitly
deferred the identity/company and personal-confirmation fields. No school submission,
new experiment, model change, download or protected-reserve scoring occurred.

## Final critical review additions

The E51–E92 bridge now explains failed real-photo expansion, fine-tuning and constrained
corrections before describing the composite E92 system. Model2 includes the full
CocoGlide/central-region comparison and the shared-COCO-origin limitation before its
small placement pilot. The report explains how PLAN, experiments, history and dataset
records guided decisions. It uses shorter score and runtime explanations, keeps exact
threshold values in research evidence and adds a cited industry comparison with Adobe
Content Credentials. This is a literature comparison, not an implemented feature.

An early DATASETS SCIMD licence/count statement was explicitly corrected against the
later publisher audit and acquisition receipt. Historical gallery calibration is not
erased by later exclusion rules: the report calls the gallery reused diagnostic data.
The critical rubric and full guideline-to-section map are in `CRITICAL_REVIEW.md`.
